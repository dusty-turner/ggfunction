% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_function_2d_1d.R
\name{geom_function_2d_1d}
\alias{geom_function_2d_1d}
\alias{stat_function_2d_1d}
\alias{StatFunction2d}
\alias{GeomFunction2d}
\alias{StatFunction2dContour}
\alias{StatFunction2dContourFilled}
\title{Visualize a Scalar Field z = f(x, y)}
\usage{
geom_function_2d_1d(
  mapping = NULL,
  data = NULL,
  stat = StatFunction2d,
  geom = GeomFunction2d,
  ...,
  position = "identity",
  fun = NULL,
  xlim = NULL,
  ylim = NULL,
  n = NULL,
  args = list(),
  type = "raster",
  bins = NULL,
  binwidth = NULL,
  breaks = NULL,
  show.legend = TRUE,
  inherit.aes = TRUE
)

stat_function_2d_1d(
  mapping = NULL,
  data = NULL,
  geom = GeomFunction2d,
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE,
  fun = NULL,
  xlim = c(-1, 1),
  ylim = c(-1, 1),
  n = 50,
  args = list()
)

StatFunction2d

GeomFunction2d

StatFunction2dContour

StatFunction2dContourFilled
}
\arguments{
\item{mapping}{Aesthetic mappings, created using \code{aes()}. If \code{NULL}, defaults are used.}

\item{data}{Optional data frame to override the default data source.}

\item{stat}{Statistical transformation to use. Defaults to \code{StatFunction2d}.}

\item{geom}{Geom used for rendering. Defaults to \code{GeomFunction2d}.}

\item{...}{Other arguments passed to the layer, such as additional parameters.}

\item{position}{Position adjustment for the layer. Defaults to \code{"identity"}.}

\item{fun}{A function evaluated at each grid point. It takes a length-2
numeric vector \code{c(x, y)} and returns a single numeric value \code{z}.}

\item{xlim}{Numeric vector of length 2 specifying the x-range of the grid. Required if \code{fun} is provided.}

\item{ylim}{Numeric vector of length 2 specifying the y-range of the grid. Required if \code{fun} is provided.}

\item{n}{Number of points in the grid along each axis. Defaults to \code{50} in \code{stat_function_2d_1d}.}

\item{args}{A named list of additional arguments passed to \code{fun}.}

\item{type}{Character. Type of visualization: \code{"raster"} (default), \code{"contour"}, or \code{"contour_filled"}.}

\item{bins}{Number of contour bins. Only used when \code{type} is \code{"contour"} or \code{"contour_filled"}.}

\item{binwidth}{Width of contour bins. Only used when \code{type} is \code{"contour"} or \code{"contour_filled"}.}

\item{breaks}{Numeric vector of specific contour break values. Only used when \code{type} is \code{"contour"} or \code{"contour_filled"}.}

\item{show.legend}{Logical. Should this layer be included in the legends? \code{NA} includes if aesthetics are mapped.}

\item{inherit.aes}{If \code{FALSE}, overrides default aesthetics rather than combining them.}

\item{na.rm}{Logical. Should missing values be removed? Defaults to \code{FALSE}.}
}
\value{
A \code{ggplot2} layer.
}
\description{
\code{geom_function_2d_1d} adds a layer that visualizes a scalar-valued function
of two variables, \eqn{z = f(x, y)}, over a grid as a raster or contour plot.
}
\section{Computed variables}{

These are calculated by the \code{stat} part of the layer and can be accessed
with \code{\link[ggplot2:after_stat]{ggplot2::after_stat()}}.
\describe{
\item{\code{after_stat(x)}}{Grid x coordinates.}
\item{\code{after_stat(y)}}{Grid y coordinates.}
\item{\code{after_stat(z)}}{Function values on the grid for raster and contour
inputs. The raster display maps \code{fill = after_stat(z)} by default.}
\item{\code{after_stat(level)}}{Contour level for \code{type = "contour"} or
\code{type = "contour_filled"}.}
\item{\code{after_stat(nlevel)}}{Contour level scaled to a maximum of 1.}
\item{\code{after_stat(piece)}}{Contour piece identifier.}
\item{\code{after_stat(level_low)}}{Lower boundary of each filled-contour band.}
\item{\code{after_stat(level_high)}}{Upper boundary of each filled-contour band.}
\item{\code{after_stat(level_mid)}}{Midpoint of each filled-contour band.}
}
}

\section{Dropped variables}{

\code{z} is used to compute contour lines or filled contour bands and is not
available after contouring.
}

\section{Aesthetics}{

\code{geom_function_2d_1d()} does not require input aesthetics when \code{fun} is
supplied. Raster layers understand \code{x}, \code{y}, \code{fill}, \code{alpha}, and \code{group};
contour layers use ggplot2's contour aesthetics, including \code{colour},
\code{linetype}, \code{linewidth}, \code{fill}, and \code{group} depending on \code{type}.
}

\examples{
# Function that calculates the norm
f <- function(v) {
  x <- v[1]
  y <- v[2]
  c(sqrt(x^2 + y^2))
}

ggplot() +
  geom_function_2d_1d(fun = f, xlim = c(-5, 5), ylim = c(-5, 5))

# Sinusoidal combination of sine and cosine
f_sin_cos <- function(v) {
  x <- v[1]
  y <- v[2]
  sin(x) * cos(y)
}

ggplot() +
  geom_function_2d_1d(fun = f_sin_cos, xlim = c(-5, 5), ylim = c(-5, 5))

# Gaussian bump function
f_gaussian <- function(v) {
  x <- v[1]
  y <- v[2]
  exp(-(x^2 + y^2) / 2)
}

ggplot() +
  geom_function_2d_1d(fun = f_gaussian, xlim = c(-5, 5), ylim = c(-5, 5))

# Radial sine wave function
f_radial_wave <- function(v) {
  x <- v[1]
  y <- v[2]
  r <- sqrt(x^2 + y^2)
  sin(r)
}

# Some functions need more resolution for clarity
ggplot() +
  geom_function_2d_1d(fun = f_radial_wave, xlim = c(-50, 50), ylim = c(-50, 50), n = 100)

# Complex combination of radial and angular components
f_complex <- function(v) {
  x <- v[1]
  y <- v[2]
  r <- sqrt(x^2 + y^2)
  theta <- atan2(y, x)
  sin(r) * cos(theta)
}

ggplot() +
  geom_function_2d_1d(fun = f_complex, xlim = c(-50, 50), ylim = c(-50, 50), n = 500)

# Spiral pattern function
f_spiral <- function(v) {
  x <- v[1]
  y <- v[2]
  r <- sqrt(x^2 + y^2)
  theta <- atan2(y, x)
  sin(r + theta)
}

ggplot() +
  geom_function_2d_1d(fun = f_spiral, xlim = c(-50, 50), ylim = c(-50, 50), n = 500)

# Parameterized scalar field via `args`
f <- function(v, a = 1, b = 1) {
  a * sin(v[1]) + b * cos(v[2])
}

ggplot() +
  geom_function_2d_1d(
    fun = f,
    xlim = c(-5, 5),
    ylim = c(-5, 5),
    args = list(a = 2, b = 0.5)
  )
}
\seealso{
\code{\link[ggplot2:geom_raster]{ggplot2::geom_raster()}} and \code{\link[ggplot2:geom_contour]{ggplot2::geom_contour()}} for the
underlying raster and contour drawing conventions.
}
